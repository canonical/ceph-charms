variables:
  openstack-origin: &openstack-origin cloud:caracal/proposed

series: &series jammy

machines:
  '0':
  '1':
  '2':
  '3':
    series: noble
  '4':
    series: noble
  '5':
    constraints: cores=2 mem=6G root-disk=40G virt-type=virtual-machine
  '6':
    constraints: cores=2 mem=6G root-disk=40G virt-type=virtual-machine
  '7':
    constraints: cores=2 mem=6G root-disk=40G virt-type=virtual-machine

applications:
  ceph-mon:
    charm: ch:ceph-mon
    num_units: 3
    channel: latest/edge
    options:
      monitor-count: 3
      source: *openstack-origin
    to:
      - '0'
      - '1'
      - '2'

  ceph-nvme:
    charm: ../../../ceph-nvme.charm
    num_units: 2
    options:
      nr-hugepages: 0
      cpuset: "2"
    series: noble
    to:
      - '3'
      - '4'

  ceph-osd:
    charm: ch:ceph-osd
    num_units: 3
    channel: latest/edge
    storage:
      osd-devices: 'loop,10G'
    options:
      source: *openstack-origin
    to:
      - '5'
      - '6'
      - '7'

relations:
  - - 'ceph-mon'
    - 'ceph-nvme'

  - - 'ceph-osd:mon'
    - 'ceph-mon:osd'
